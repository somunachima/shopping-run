<h1>My Shopping List</h1>

<div class="main-list">
  <% @items.each do |item| %>
    <%# <li><%= link_to item.name, items_path(item)  %>
    <li><%= item.name %></li>
  <% end %>
</div>

<p><%= link_to "Add a new item", "/items/new"  %></p>
